import { GrammarLevelPicker } from '../components/grammar/GrammarLevelPicker'

/** /grammar — the level-selection entry point for the Phase 4 grammar system (spec section 2). */
export function GrammarIndexPage() {
  return (
    <section>
      <GrammarLevelPicker />
    </section>
  )
}
