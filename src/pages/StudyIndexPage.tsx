import { StudyLevelPicker } from '../components/study/StudyLevelPicker'

/** /study — the level-selection entry point for the Phase 3 flashcard system (spec section 2). */
export function StudyIndexPage() {
  return (
    <section>
      <StudyLevelPicker />
    </section>
  )
}
